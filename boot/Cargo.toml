[package]
name = "boot"
version = "0.1.0"
edition = "2021"
publish = false

[dependencies]
bootloader = "0.11.2"

[build-dependencies]
bootloader = { version = "0.11.2", features = ["uefi"] }

[package.metadata.bootloader]
target = "x86_64-unknown-none"
